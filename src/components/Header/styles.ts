import styled from 'styled-components';

export const HeaderWrapper = styled.div`
    position: sticky;
    top: 0;
    height: 40px;
    display: flex;
    gap: 24px;
    background-color: goldenrod;
    color: black;
    padding: 8px;
`;
